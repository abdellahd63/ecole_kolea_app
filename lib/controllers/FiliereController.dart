import 'package:get/get.dart';

class FiliereController extends GetxController {
  var selectedFiliere = ''.obs;

  @override
  void onInit() {
    // Initialize variables or perform other setup operations
    super.onInit();
  }

  @override
  void onClose() {
    // Clean up resources when the controller is closed
    super.onClose();
  }
}